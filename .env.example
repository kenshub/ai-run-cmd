# === AI RUN CMD Configuration ===

# AI Provider: openai, ollama, anthropic, mistral, or groq
AI_PROVIDER=openai

# Default action after command selection: run, copy, or ask
DEFAULT_ACTION=ask

# OpenAI API Key (used by 'ai' command)
OPENAI_API_KEY="your-api-key-here"

# Preferred OpenAI model
OPENAI_MODEL=gpt-3.5-turbo

# Optional context prepended to every prompt
AI_CONTEXT="Act like a terminal assistant. I'm using Linux Mint and Bash. Always respond with full terminal commands. No explanations unless I ask. Warn me if the command is dangerous."

AI_OS="Linux Mint"
AI_SHELL="bash"


# Ollama model to use locally when AI_PROVIDER is set to 'ollama'
OLLAMA_MODEL=mistral

# Anthropic API Key (used when AI_PROVIDER is set to 'anthropic')
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# Preferred Claude model
CLAUDE_MODEL=claude-3-opus

# Mistral API Key (used when AI_PROVIDER is set to 'mistral')
MISTRAL_API_KEY="your-mistral-api-key-here"

# Preferred Mistral model
MISTRAL_MODEL=mistral-tiny

# Groq API Key (used when AI_PROVIDER is set to 'groq')
GROQ_API_KEY="your-groq-api-key-here"

# Preferred Groq model
GROQ_MODEL=llama3-70b

# Enable debug output (1 = on, 0 = off)
DEBUG_AI=0

# === AI RUN CMD Configuration ===

# OpenAI API Key (used by 'ai' command)
OPENAI_API_KEY=your-openai-api-key-here

# Preferred OpenAI model
OPENAI_MODEL=gpt-3.5-turbo

# Ollama model to use locally with 'ail'
OLLAMA_MODEL=mistral

# Default AI engine: openai or ollama (optional toggle for future logic)
DEFAULT_AI_ENGINE=openai

# Enable debug output (1 = on, 0 = off)
DEBUG_AI=0

# Default action after command selection: run, copy, or ask
DEFAULT_ACTION=ask
